root: "/var/www/vhosts"
git-wrapper:
  git-binary:
  env-vars:
virtual-host:
  default:
    index:
      exclude:
        - "git-checker"
        - "lost+found"
    show:
      depth: "< 4"
      exclude:
    add:
      allow:
      exclude:
    umask:
      file:
        user:
        group:
        others:
      folder:
        user:
        group:
        others:
